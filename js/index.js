var arr = ['red', 'orange', 
'yellow', 'green', 'blue', 'indigo', 
 'violet'];
 var count = 0;

var cycle = function(array/*perameter*/) {
	console.log(array[count]);
	

	console.log(array[count]);
	count++; // or count += 1; or count = count + 
};

var list3 = function() {
	cycle(arr);
}